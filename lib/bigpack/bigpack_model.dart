import 'package:equatable/equatable.dart';

/// generate by https://javiercbk.github.io/json_to_dart/
class AutogeneratedBigpack {
  List<BigpackModel> results;

  AutogeneratedBigpack({this.results});

  AutogeneratedBigpack.fromJson(Map<String, dynamic> json) {
    if (json['results'] != null) {
      results = new List<BigpackModel>();
      json['results'].forEach((v) {
        results.add(new BigpackModel.fromJson(v));
      });
    }
  }

  Map<String, dynamic> toJson() {
    final Map<String, dynamic> data = new Map<String, dynamic>();
    if (this.results != null) {
      data['results'] = this.results.map((v) => v.toJson()).toList();
    }
    return data;
  }
}

class BigpackModel extends Equatable {
  int id;
  String name;

  BigpackModel({this.id, this.name}) : super([id, name]);

  /// use : super([json]) for Equatable
  BigpackModel.fromJson(Map<String, dynamic> json) : super([json]) {
    id = json['id'];
    name = json['name'];
  }

  Map<String, dynamic> toJson() {
    final Map<String, dynamic> data = new Map<String, dynamic>();
    data['id'] = this.id;
    data['name'] = this.name;
    return data;
  }
}
